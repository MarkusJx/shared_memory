{
  "name": "@markusjx/shared_memory",
  "version": "1.0.1",
  "description": "Shared memory for node.js",
  "main": "index.js",
  "scripts": {
    "build:dev": "cmake-js compile -D",
    "build": "cmake-js build",
    "test": "mocha",
    "pretest": "npm run build",
    "install": "npm run build"
  },
  "author": "MarkusJx",
  "license": "MIT",
  "dependencies": {
    "@markusjx/n-api-tools": "^1.1.1",
    "cmake-js": "^6.3.0",
    "node-addon-api": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.35",
    "mocha": "^10.0.0"
  }
}
